<%= form_tag(supermarkets_path, :method => "get") do %>
  <div class="form-group">
    <%= text_field_tag :search, params[:search], :class => "col-sm-3", placeholder: "Digite seu CEP" %><br><br>
    <%= submit_tag "Buscar", :name => nil, :class => 'btn-sm btn-success' %>
  </div>
<% end %>
